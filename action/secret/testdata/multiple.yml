---
metadata:
  version: "1"
  engine: native
secrets:
  - org: github
    repo: octocat
    name: multiple-events
    value: bar
    type: repo
    events:
      - comment
      - deployment
      - pull_request
      - push
      - tag
      - release

---
metadata:
  version: "1"
  engine: native
secrets:
  - org: github
    repo: octocat
    name: multiple-images
    value: bar
    type: repo
    images:
      - alpine
      - golang:latest
      - postgres:latest
